# Example environment variables for HospiLink
# Copy this file to .env.local (preferred for Next.js) or .env and fill in values.

# Public API base URL (exposed to browser). Include protocol and no trailing slash.
NEXT_PUBLIC_API_URL=http://localhost:1115

# (Optional) Google OAuth Client ID
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Add any other NEXT_PUBLIC_ variables here
